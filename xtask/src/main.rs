const USAGE: &str = "xtask — repository automation for ethp2p-rs

USAGE:
    cargo xtask <COMMAND>

COMMANDS:
    (none yet — this is a skeleton; subcommands land with later slices)

OPTIONS:
    -h, --help    Print this message
";

fn main() {
    print!("{USAGE}");
}
