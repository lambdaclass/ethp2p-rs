//! Rust-native simulation harness for ethp2p.
//!
//! Stub crate. Slice 5 (`port-sim-harness`) selects between `madsim` and
//! `turmoil` and lands the harness here. This crate is NOT a port of the
//! Go `sim/` package; it is a parallel Rust effort. The Go simnet driver
//! depends on Go's `testing/synctest`, which has no Rust equivalent.
